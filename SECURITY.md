# 安全策略

## 支持范围

Echo Companion 当前处于 `0.x` 早期开发阶段。安全修复以 `main` 分支和最新公开版本（如有）为目标；旧版本不保证继续获得补丁。升级或发布前请查看对应版本的发布说明。

## 报告漏洞

如果仓库的 **Security → Report a vulnerability** 可用，请优先使用 GitHub 私密漏洞报告。若该入口尚未启用，请创建一个不含利用细节、API Key 或个人数据的最小化 Issue，请求维护者建立私密沟通渠道。

报告中建议包含：

- 受影响的提交或版本，以及 Fabric / NeoForge 加载器版本；
- 可复现的最小步骤和预期影响；
- 已做脱敏的日志或请求结构；
- 是否存在公开利用，以及建议的临时缓解措施。

请勿在公开 Issue、Pull Request、截图或日志中提交真实 API Key、`Authorization` 请求头、会话内容或私人 endpoint。若 Key 已泄露，请立即在提供商处撤销并重新生成；仅从仓库历史删除字符串并不能使 Key 失效。

## 当前信任边界

- `SCRIPTED` 模式的对话引擎不调用远程 AI 服务。
- `REMOTE` 模式会向玩家填写的 endpoint 发送对话消息，并在 Key 非空时发送 Bearer 凭据。远程服务处于本项目的信任边界之外。
- 公网 endpoint 仅允许 HTTPS；HTTP 仅允许精确的本机回环主机。endpoint 拒绝用户信息、查询参数与片段，HTTP 重定向也被禁用，避免凭据通过 URL 落盘或被转发到重定向目标。
- 默认不将 Key 写入磁盘。开启“在本机记住 Key”后，Key 会以未加密明文保存在游戏实例的 `config/echo_companion-client.json`。
- 同一操作系统账户、同一 JVM 内的其他模组以及能读取该配置文件的程序，都可能访问已记住的 Key。界面中的遮罩仅防止旁观者直接看到文本，不是加密或安全隔离。
- 配置与对话状态保存在客户端，不通过 Minecraft 网络包交给服务器。模型回复没有服务器命令、实体控制或世界写入能力。

用户应只配置可信 endpoint，并优先使用可撤销、有限权限和有限额度的专用 Key。

## 不属于漏洞的情况

以下行为是当前明确披露的设计限制，通常不视为安全漏洞：

- 用户主动开启记住功能后，Key 在本机配置文件中明文保存；
- 第三方 API 服务记录、处理或计费其收到的请求；
- 恶意客户端模组读取同一进程或同一账户可访问的数据；
- 模型生成错误、不当或虚构内容，但没有跨越客户端对话边界执行操作。

如果这些限制可被绕过并导致未授权发送、凭据泄漏或世界状态改变，请按漏洞报告流程提交。
